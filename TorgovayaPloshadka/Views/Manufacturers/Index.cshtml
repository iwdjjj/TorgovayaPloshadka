@model IEnumerable<TorgovayaPloshadka.Models.Manufacturer>

@{
    ViewData["Title"] = "Index";
}

<h1>Производители</h1>

<p>
    <a asp-action="Create">Добавить производителя</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Midname)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Midname)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.ManufacturerId">Редактировать</a> |
                    <a asp-action="Details" asp-route-id="@item.ManufacturerId">Детали</a> |
                    <a asp-action="Delete" asp-route-id="@item.ManufacturerId">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
